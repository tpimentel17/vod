import { SearchVideoPipe } from './search-video.pipe';

describe('SearchVideoPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchVideoPipe();
    expect(pipe).toBeTruthy();
  });
});
